services:
  nightlight_api:
    build:
      context: /srv/volumes/nightlight_api
      dockerfile: Dockerfile
    restart: unless-stopped
    expose:
      - "${PORT}:5000"
    env_file:
      - /srv/nightline01/environments/nightlight_api.env
    volumes:
      -  "/srv/volumes/nightlight_api:/app"
    healthcheck:
      test: ["CMD", "curl", "--fail", "http://localhost:5000"]
      interval: 300s
      retries: 3
      start_period: 10s
      timeout: 10s
    networks:
      - reverseproxy

volumes:
  db_instances:

networks:
  reverseproxy:
    external: true
